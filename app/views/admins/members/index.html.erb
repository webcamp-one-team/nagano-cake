<div class="row">
  <div class="col-xs-12">
    <h2>会員一覧</h2>
    <table class="table table-hover table-inverse">
      <thead>
        <tr>
          <th>会員ID</th>
          <th>氏名</th>
          <th>メールアドレス</th>
          <th>ステータス</th>
        </tr>
      </thead>
      <tbody>
        <% @members.each do |member| %>
        <tr>
          <td><%= member.id %></td>
          <td><%= link_to "#{member.last_name} #{member.first_name}", admins_member_path(member.id)%></td>
          <td><%= member.email %></td>
          <% if member.is_deleted == true %>
          <td>退会済</td>
          <% else %>
          <td>有効</td>
          <% end %>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>