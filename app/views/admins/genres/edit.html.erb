<h2>ジャンル編集</h2>
<div class="row">
	<div class="col-xs-9">
		<%= form_for @genre, url: admins_genre_path do |f| %>
			<h4>ジャンル</h4>
			<%= f.text_field :name %>
			<%= f.select :validity ,[['有効',true],['無効',false]] %>
			<%= f.submit '変更を保存する' %>
		<% end %>
	</div>
</div>